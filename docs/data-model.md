# Data Model

## Source Inventory

The `yelp_dataset/` directory contains five JSON Lines files:

1. `yelp_academic_dataset_business.json`
2. `yelp_academic_dataset_review.json`
3. `yelp_academic_dataset_tip.json`
4. `yelp_academic_dataset_checkin.json`
5. `yelp_academic_dataset_user.json`

Each line is one JSON object.

## Field Inventory

### Business

Core fields observed in `yelp_academic_dataset_business.json`:

- `business_id`
- `name`
- `address`
- `city`
- `state`
- `postal_code`
- `latitude`
- `longitude`
- `stars`
- `review_count`
- `is_open`
- `attributes`
- `categories`
- `hours`

Milestone 1 relevance:

- Required now:
  - `business_id`
  - `name`
  - `city`
  - `state`
  - `stars`
  - `review_count`
  - `categories`
- Useful for later:
  - `attributes`
  - `hours`
  - `address`
  - `is_open`

### Review

Core fields observed in `yelp_academic_dataset_review.json`:

- `review_id`
- `user_id`
- `business_id`
- `stars`
- `useful`
- `funny`
- `cool`
- `text`
- `date`

Milestone 1 relevance:

- Required now:
  - `review_id`
  - `business_id`
  - `stars`
  - `text`
  - `date`
- Optional now:
  - `user_id`
  - `useful`
  - `funny`
  - `cool`

### Tip

Core fields observed in `yelp_academic_dataset_tip.json`:

- `user_id`
- `business_id`
- `text`
- `date`
- `compliment_count`

Milestone 1 decision:

- Excluded from the first demo retrieval path.
- Can be merged later as short-form supporting evidence.

### Check-in

Core fields observed in `yelp_academic_dataset_checkin.json`:

- `business_id`
- `date`

Milestone 1 decision:

- Excluded from the first demo retrieval path.
- May be used later for crowding and popularity heuristics.

### User

Core fields observed in `yelp_academic_dataset_user.json`:

- `user_id`
- `name`
- `review_count`
- `yelping_since`
- `useful`
- `funny`
- `cool`
- `elite`
- `friends`
- `fans`
- `average_stars`
- `compliment_hot`
- `compliment_more`
- `compliment_profile`
- `compliment_cute`
- `compliment_list`
- `compliment_note`
- `compliment_plain`
- `compliment_cool`
- `compliment_funny`
- `compliment_writer`
- `compliment_photos`

Milestone 1 decision:

- Excluded from the serving path.
- Kept as future enrichment material for reviewer credibility features.

## Milestone 1 Storage Shape

For the first milestone, the system serves from a deterministic demo subset stored in:

- `backend/data/samples/demo_businesses.jsonl`
- `backend/data/samples/demo_reviews.jsonl`
- `backend/data/samples/demo_metadata.json`

These files are generated by:

- `scripts/build_sample_dataset.py`

## Review Cleaning Plan

The first-stage cleaning plan is intentionally minimal and reversible:

1. Preserve original review text in storage.
2. Normalize line endings and whitespace at read time only.
3. Keep original star rating and review date.
4. Do not remove punctuation, emojis, or casing in the raw stored payload.
5. Defer sentence splitting, aspect extraction, and sentiment normalization to Milestone 3.

## Retrieval Strategy in Milestone 1

Milestone 1 only needs reliable restaurant and review retrieval. The serving layer therefore:

1. Loads the demo business subset into memory keyed by `business_id`.
2. Loads the demo review subset into memory grouped by `business_id`.
3. Returns restaurant summaries and review lists directly from those cached structures.

This avoids premature database work while still meeting the milestone exit criterion.

## Example Review-Centric Record

The current database-backed structure in Milestone 2 can be understood from the perspective of one review.

Example review from the demo subset:

```json
{
  "review_id": "eSokG6KmGZN_9exB4dac3A",
  "user_id": "_zEEc8nF7jtmzi_m8CYHug",
  "business_id": "ytynqOUb3hjKeJfRj5Tshw",
  "stars": 5.0,
  "useful": 0,
  "funny": 0,
  "cool": 0,
  "text": "I love it I played with my friends 24 hours and the food is bussin bussin you should try this",
  "date": "2022-01-19 17:34:23"
}
```

In the database, this review maps into the `reviews` table and links to the rest of the model like this:

### 1. `reviews`

Primary row:

- `review_id`: `eSokG6KmGZN_9exB4dac3A`
- `user_id`: `_zEEc8nF7jtmzi_m8CYHug`
- `business_id`: `ytynqOUb3hjKeJfRj5Tshw`
- `stars`: `5.0`
- `text`: the full review text
- `review_date`: `2022-01-19 17:34:23`

### 2. `restaurants`

The `business_id` foreign key points to this restaurant row:

- `business_id`: `ytynqOUb3hjKeJfRj5Tshw`
- `name`: `Reading Terminal Market`
- `city`: `Philadelphia`
- `state`: `PA`
- `stars`: `4.5`
- `review_count`: `5721`
- `categories`: JSON array version of the source categories

This is the main join path used by the current app:

```sql
SELECT r.review_id, r.text, r.stars, rt.name, rt.city, rt.state
FROM reviews r
JOIN restaurants rt ON r.business_id = rt.business_id
WHERE r.review_id = 'eSokG6KmGZN_9exB4dac3A';
```

### 3. `restaurant_aspect_signals`

The same `business_id` also links to the placeholder precomputed aspect table:

- `business_id`: `ytynqOUb3hjKeJfRj5Tshw`
- `overall_rating`: currently seeded from restaurant stars
- `food_score`
- `service_score`
- `price_score`
- `ambience_score`
- `waiting_time_score`
- `pros`
- `cons`
- `risk_flags`

That means one review currently sits in this relationship chain:

```text
reviews.review_id
  -> reviews.business_id
  -> restaurants.business_id
  -> restaurant_aspect_signals.business_id
```

### Practical meaning

Using one review as the center point, the current structure already supports:

- fetching the review text and metadata
- knowing which restaurant the review belongs to
- showing restaurant-level summary data next to the review
- attaching future precomputed aspect/risk summaries at the restaurant level

The current schema does not yet attach per-review aspect annotations. Those would be added later as a separate review-level analysis table rather than overloading `reviews`.
