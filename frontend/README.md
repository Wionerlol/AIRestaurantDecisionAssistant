# Frontend

Next.js frontend for the AI Restaurant Decision Assistant.

## Run

```bash
npm install
npm run dev
```

To point the chat page at a different backend, set:

```bash
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
```
